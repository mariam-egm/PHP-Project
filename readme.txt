upload to remote repository:

git add . (kolo/file name)
git commit -m "change-message"
git push origin master (kda ramet fel site)
 
 then pull request on website

the other side :

git pull origin master

//FIXME:
//TODO: